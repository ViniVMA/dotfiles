# Place a copy of this config to ~/.aerospace.toml
# After that, you can edit ~/.aerospace.toml to your liking

# You can use it to add commands that run after AeroSpace startup.
# Available commands : https://nikitabobko.github.io/AeroSpace/commands

# Start AeroSpace at login
start-at-login = true

after-startup-command = [
  'exec-and-forget borders active_color=0x66808080 inactive_color=0x00000000 width=6.0 style=round',
  'workspace 𝙲̲ommunication',
  'layout accordion horizontal',
  'workspace 𝙳̲evelopment',
]

# Normalizations. See: https://nikitabobko.github.io/AeroSpace/guide#normalization
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# See: https://nikitabobko.github.io/AeroSpace/guide#layouts
# The 'accordion-padding' specifies the size of accordion padding
# You can set 0 to disable the padding feature
accordion-padding = 30

# Possible values: tiles|accordion
default-root-container-layout = 'tiles'

# Possible values: horizontal|vertical|auto
# 'auto' means: wide monitor (anything wider than high) gets horizontal orientation,
#               tall monitor (anything higher than wide) gets vertical orientation
default-root-container-orientation = 'auto'

# Mouse follows focus when focused monitor changes
# Drop it from your config, if you don't like this behavior
# See https://nikitabobko.github.io/AeroSpace/guide#on-focus-changed-callbacks
# See https://nikitabobko.github.io/AeroSpace/commands#move-mouse
# Fallback value (if you omit the key): on-focused-monitor-changed = []
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']

# You can effectively turn off macOS "Hide application" (cmd-h) feature by toggling this flag
# Useful if you don't use this macOS feature, but accidentally hit cmd-h or cmd-alt-h key
# Also see: https://nikitabobko.github.io/AeroSpace/goodies#disable-hide-app
automatically-unhide-macos-hidden-apps = true

exec-on-workspace-change = ['/bin/bash', '-c',
    'sketchybar --trigger aerospace_workspace_change FOCUSED_WORKSPACE=$AEROSPACE_FOCUSED_WORKSPACE PREV_WORKSPACE=$AEROSPACE_PREV_WORKSPACE'
]

# Possible values: (qwerty|dvorak|colemak)
# See https://nikitabobko.github.io/AeroSpace/guide#key-mapping
[key-mapping]
preset = 'qwerty'

# Gaps between windows (inner-*) and between monitor edges (outer-*).
# Possible values:
# - Constant:     gaps.outer.top = 8
# - Per monitor:  gaps.outer.top = [{ monitor.main = 16 }, { monitor."some-pattern" = 32 }, 24]
#                 In this example, 24 is a default value when there is no match.
#                 Monitor pattern is the same as for 'workspace-to-monitor-force-assignment'.
#                 See:
#                 https://nikitabobko.github.io/AeroSpace/guide#assign-workspaces-to-monitors
[gaps]
inner.horizontal = 10
inner.vertical = 0
outer.left = 0
outer.bottom = 0
outer.top = 0
outer.right = 0

# 'main' binding mode declaration
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
# 'main' binding mode must be always presented
# Fallback value (if you omit the key): mode.main.binding = {}
[mode.main.binding]

# All possible keys:
# - Letters.        a, b, c, ..., z
# - Numbers.        0, 1, 2, ..., 9
# - Keypad numbers. keypad0, keypad1, keypad2, ..., keypad9
# - F-keys.         f1, f2, ..., f20
# - Special keys.   minus, equal, period, comma, slash, backslash, quote, semicolon,
#                   backtick, leftSquareBracket, rightSquareBracket, space, enter, esc,
#                   backspace, tab, pageUp, pageDown, home, end, forwardDelete,
#                   sectionSign (ISO keyboards only, european keyboards only)
# - Keypad special. keypadClear, keypadDecimalMark, keypadDivide, keypadEnter, keypadEqual,
#                   keypadMinus, keypadMultiply, keypadPlus
# - Arrows.         left, down, up, right

# All possible modifiers: cmd, alt, ctrl, shift

# All possible commands: https://nikitabobko.github.io/AeroSpace/commands

# See: https://nikitabobko.github.io/AeroSpace/commands#exec-and-forget
# You can uncomment the following lines to open up terminal with alt + enter shortcut
# (like in i3)
# alt-enter = '''exec-and-forget osascript -e '
# tell application "Terminal"
#     do script
#     activate
# end tell'
# '''

# See: https://nikitabobko.github.io/AeroSpace/commands#layout
# Moved to service mode (Hyper+; then / or ,) since punctuation keys
# don't register reliably with cmd+alt+ctrl+shift

# See: https://nikitabobko.github.io/AeroSpace/commands#focus
cmd-alt-ctrl-shift-h = 'focus left'
cmd-alt-ctrl-shift-j = 'focus down'
cmd-alt-ctrl-shift-k = 'focus up'
cmd-alt-ctrl-shift-l = 'focus right'

# See: https://nikitabobko.github.io/AeroSpace/commands#resize
cmd-alt-ctrl-shift-minus = 'resize smart -50'
cmd-alt-ctrl-shift-equal = 'resize smart +50'

# See: https://nikitabobko.github.io/AeroSpace/commands#workspace
cmd-alt-ctrl-shift-b = 'workspace 𝙱̲rowsing'
cmd-alt-ctrl-shift-c = 'workspace 𝙲̲ommunication'
cmd-alt-ctrl-shift-d = 'workspace 𝙳̲evelopment'
cmd-alt-ctrl-shift-e = 'workspace 𝙴̲xtra'
cmd-alt-ctrl-shift-f = 'workspace 𝙵̲ile_Management'
cmd-alt-ctrl-shift-g = 'workspace 𝙶̲aming'
cmd-alt-ctrl-shift-m = 'workspace 𝙼̲usic'
cmd-alt-ctrl-shift-o = 'workspace 𝙾̲ther'
cmd-alt-ctrl-shift-p = 'workspace 𝙿̲lanning'
cmd-alt-ctrl-shift-1 = 'workspace Project_𝟷̲'
cmd-alt-ctrl-shift-2 = 'workspace Project_𝟸̲'
cmd-alt-ctrl-shift-r = 'workspace 𝚁̲esearch'
cmd-alt-ctrl-shift-s = 'workspace 𝚂̲ystem_Administration'
cmd-alt-ctrl-shift-w = 'workspace 𝚆̲riting'
cmd-alt-ctrl-shift-v = 'workspace 𝚅̲M'

# # See: https://nikitabobko.github.io/AeroSpace/commands#workspace
# alt-1 = 'workspace 1'
# alt-2 = 'workspace 2'
# alt-3 = 'workspace 3'
# alt-4 = 'workspace 4'
# alt-5 = 'workspace 5'
# alt-6 = 'workspace 6'
# alt-7 = 'workspace 7'
# alt-8 = 'workspace 8'
# alt-9 = 'workspace 9'
#
#
# # See: https://nikitabobko.github.io/AeroSpace/commands#move-node-to-workspace
# alt-shift-1 = 'move-node-to-workspace 1'
# alt-shift-2 = 'move-node-to-workspace 2'
# alt-shift-3 = 'move-node-to-workspace 3'
# alt-shift-4 = 'move-node-to-workspace 4'
# alt-shift-5 = 'move-node-to-workspace 5'
# alt-shift-6 = 'move-node-to-workspace 6'
# alt-shift-7 = 'move-node-to-workspace 7'
# alt-shift-8 = 'move-node-to-workspace 8'
# alt-shift-9 = 'move-node-to-workspace 9'


# See: https://nikitabobko.github.io/AeroSpace/commands#workspace-back-and-forth
cmd-alt-ctrl-shift-space = 'workspace-back-and-forth'
cmd-alt-ctrl-shift-backtick = '''exec-and-forget bash -c '\
  osascript -e "tell application \"System Events\" to set autohide of dock preferences to not (autohide of dock preferences)" \
  && aerospace flatten-workspace-tree'
'''

# See: https://nikitabobko.github.io/AeroSpace/commands#mode
cmd-alt-ctrl-shift-a = 'mode service'

# 'service' binding mode declaration.
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
[mode.service.binding]
esc = ['reload-config', 'mode main']
slash = ['layout tiles horizontal vertical', 'mode main']
comma = ['layout accordion horizontal vertical', 'mode main']
r = ['flatten-workspace-tree', 'mode main'] # reset layout
f = [
  'layout floating tiling',
  'mode main',
] # Toggle between floating and tiling layout
backspace = ['close-all-windows-but-current', 'mode main']

# sticky is not yet supported https://github.com/nikitabobko/AeroSpace/issues/2
#s = ['layout sticky tiling', 'mode main']

# Move window (was alt-shift-h/j/k/l)
h = ['move left', 'mode main']
j = ['move down', 'mode main']
k = ['move up', 'mode main']
l = ['move right', 'mode main']

# Join with (was alt-shift-h/j/k/l in service mode)
shift-h = ['join-with left', 'mode main']
shift-j = ['join-with down', 'mode main']
shift-k = ['join-with up', 'mode main']
shift-l = ['join-with right', 'mode main']

# Move node to workspace (was alt-shift-<letter>)
b = ['move-node-to-workspace 𝙱̲rowsing --focus-follows-window', 'mode main']
c = ['move-node-to-workspace 𝙲̲ommunication --focus-follows-window', 'mode main']
d = ['move-node-to-workspace 𝙳̲evelopment --focus-follows-window', 'mode main']
e = ['move-node-to-workspace 𝙴̲xtra --focus-follows-window', 'mode main']
g = ['move-node-to-workspace 𝙶̲aming --focus-follows-window', 'mode main']
m = ['move-node-to-workspace 𝙼̲usic --focus-follows-window', 'mode main']
o = ['move-node-to-workspace 𝙾̲ther --focus-follows-window', 'mode main']
p = ['move-node-to-workspace 𝙿̲lanning --focus-follows-window', 'mode main']
1 = ['move-node-to-workspace Project_𝟷̲ --focus-follows-window', 'mode main']
2 = ['move-node-to-workspace Project_𝟸̲ --focus-follows-window', 'mode main']
s = ['move-node-to-workspace 𝚂̲ystem_Administration --focus-follows-window', 'mode main']
w = ['move-node-to-workspace 𝚆̲riting --focus-follows-window', 'mode main']
v = ['move-node-to-workspace 𝚅̲M --focus-follows-window', 'mode main']
shift-f = ['move-node-to-workspace 𝙵̲ile_Management --focus-follows-window', 'mode main']
shift-r = ['move-node-to-workspace 𝚁̲esearch --focus-follows-window', 'mode main']
t = ['move-workspace-to-monitor --wrap-around next', 'mode main']

down = 'volume down'
up = 'volume up'
shift-down = ['volume set 0', 'mode main']


# Wezterm
[[on-window-detected]]
if.app-id = 'com.github.wez.wezterm'
# if.during-aerospace-startup = true
run = ['layout tiling', 'move-node-to-workspace 𝙳̲evelopment']


# Wezterm
[[on-window-detected]]
if.app-id = 'dev.zed.Zed'
# if.during-aerospace-startup = true
run = ['layout tiling', 'move-node-to-workspace 𝙳̲evelopment']

#Google Chrome
[[on-window-detected]]
if.app-id = 'com.google.Chrome'
run = ['layout tiling', 'move-node-to-workspace 𝙳̲evelopment']

# Bitwarden
[[on-window-detected]]
if.app-id = 'com.bitwarden.desktop'
# if.during-aerospace-startup = true
run = ['layout tiling', 'move-node-to-workspace 𝙴̲xtra']


# Zen Browser
[[on-window-detected]]
if.app-id = 'app.zen-browser.zen'
# if.during-aerospace-startup = true
run = ['layout tiling', 'move-node-to-workspace 𝙱̲rowsing']


# Slack
[[on-window-detected]]
if.app-id = 'com.tinyspeck.slackmacgap'
run = ['layout tiling', 'move-node-to-workspace 𝙲̲ommunication']

# Notion Calendar
[[on-window-detected]]
if.app-id = 'com.cron.electron'
run = ['layout tiling', 'move-node-to-workspace 𝙲̲ommunication']


# Zoom
[[on-window-detected]]
if.app-id = 'us.zoom.xos'
run = ['layout tiling', 'move-node-to-workspace 𝙲̲ommunication']


# Spotify
[[on-window-detected]]
if.app-id = 'com.spotify.client'
# if.during-aerospace-startup = true
run = ['layout tiling', 'move-node-to-workspace 𝙼̲usic']


# Finder
[[on-window-detected]]
if.app-id = 'com.apple.finder'
# if.during-aerospace-startup = true
run = ['layout tiling', 'move-node-to-workspace 𝙵̲ile_Management']


[[on-window-detected]]
if.app-id = 'com.apple.Preview'
# if.during-aerospace-startup = true
run = ['layout tiling', 'move-node-to-workspace 𝙵̲ile_Management']


# System Preferences
[[on-window-detected]]
if.app-id = 'com.apple.systempreferences'
# if.during-aerospace-startup = true
run = ['layout tiling', 'move-node-to-workspace 𝚂̲ystem_Administration']


# App Store
[[on-window-detected]]
if.app-id = 'com.apple.AppStore'
# if.during-aerospace-startup = true
run = ['layout tiling', 'move-node-to-workspace 𝚂̲ystem_Administration']


# TextEdit
[[on-window-detected]]
if.app-id = 'com.apple.TextEdit'
if.during-aerospace-startup = true
run = ['layout tiling', 'move-node-to-workspace 𝙿̲lanning']

# Todoist
[[on-window-detected]]
if.app-id = 'com.todoist.mac.Todoist'
if.during-aerospace-startup = true
run = ['layout tiling', 'move-node-to-workspace 𝙿̲lanning']
